// array methods in javascript

let color = ["yellow", "black", "white"]

console.log(color.push("pink")); // push methods just added pink color but return the total value 4
console.log(color.join()) // join methods added the color at the end before we push 